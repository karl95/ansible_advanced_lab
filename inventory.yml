# Ansible controller (VM1)
[control]
localhost ansible_connection=local

# Web server (VM2) 
[webservers]
web1 ansible_host=10.0.22.104 ansible_user=ansible

# Kõik serverid
[all:children]
control
webservers

---
all:
  children:
    webservers:
      hosts:
        web01:
          ansible_host: 10.0.22.104   # Sinu Ubuntu 2 IP
          ansible_user: ansible
